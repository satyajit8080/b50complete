/**
 * FinEdge response types.
 * Fill these in as you provide real endpoint docs — keeping `unknown` as a
 * safe default so nothing downstream breaks before the real shape is known.
 */

export interface FinEdgeCompanyProfile {
  [key: string]: unknown;
}

export interface FinEdgeFinancialStatement {
  [key: string]: unknown;
}

export interface FinEdgeRatios {
  [key: string]: unknown;
}

export interface FinEdgeShareholdingPattern {
  [key: string]: unknown;
}

export interface FinEdgeCorporateAction {
  [key: string]: unknown;
}
